#pragma once

#include "Animal.h"

class Forest {
    static const int MAX_ANIMALS = 30;  // Максимален капацитет на масива
    Animal* animals[MAX_ANIMALS];       // Фиксиран масив от указатели към животни
    int animalCount;                    // Брой добавени животни

public:
    // Конструктор по подразбиране – инициализира масива и брояча
    Forest();

    // Деструктор – освобождава паметта за всички животни в масива
    ~Forest();

    // Метод, който отпечатва информация за всички животни в леса
    void PrintInfo() const;

    // Метод, който добавя ново животно в масива, ако има свободно място
    void addAnimal(Animal* a);

    // Метод, който отстранява (премахва) животно от масива по даден индекс
    void removeAnimal(int index);

    // Метод, който симулира един ден – за всяко животно извиква неговия метод Eat
    void simulateDay();
};